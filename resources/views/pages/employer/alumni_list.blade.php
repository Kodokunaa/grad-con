@extends('layouts.authenticated')
@section('title', 'Alumni Directory · GradConn')
@push('styles')
<style>
.directory{max-width:1180px;margin:auto}.directory-hero{padding:26px;margin-bottom:18px;border-radius:22px;background:linear-gradient(135deg,#172033,#334155 60%,#f97316);color:#fff}.directory-hero h1{margin:5px 0;font-size:31px}.directory-hero p{margin:0;color:#e2e8f0}.directory-eyebrow{color:#fed7aa;font-size:12px;font-weight:900;letter-spacing:.08em;text-transform:uppercase}.notice{margin-bottom:16px;padding:12px 15px;border-radius:12px;background:#dcfce7;color:#166534;font-weight:700}.directory-filters{display:grid;grid-template-columns:2fr 1fr 1fr auto;gap:10px;padding:16px;margin-bottom:18px;border:1px solid #e2e8f0;border-radius:17px;background:#fff}.directory-filters input,.directory-filters select,.offer-form input,.offer-form textarea{width:100%;box-sizing:border-box;padding:11px 12px;border:1px solid #cbd5e1;border-radius:10px;background:#fff}.directory-filters button,.primary-btn{padding:11px 17px;border:0;border-radius:10px;background:#f97316;color:#fff;font-weight:800;cursor:pointer}.directory-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:15px}.alumni-card{padding:18px;border:1px solid #e2e8f0;border-radius:18px;background:#fff;box-shadow:0 5px 18px rgba(15,23,42,.05)}.alumni-card__top{display:flex;gap:11px;align-items:center}.alumni-initial{display:grid;width:46px;height:46px;flex:0 0 46px;place-items:center;border-radius:50%;background:linear-gradient(135deg,#fb923c,#ea580c);color:#fff;font-weight:900}.name-btn{padding:0;border:0;background:none;color:#172033;font-size:17px;font-weight:800;text-align:left;cursor:pointer}.name-btn:hover{color:#ea580c;text-decoration:underline}.alumni-card__course{margin-top:3px;color:#64748b;font-size:12px}.alumni-meta{display:flex;flex-wrap:wrap;gap:6px;margin:14px 0}.alumni-meta span{padding:5px 8px;border-radius:999px;background:#fff7ed;color:#c2410c;font-size:11px;font-weight:800}.alumni-card h3{margin:13px 0 5px;color:#475569;font-size:11px;text-transform:uppercase}.alumni-card p{margin:0;color:#475569;font-size:13px;line-height:1.55}.view-btn{width:100%;margin-top:16px;padding:9px;border:1px solid #fed7aa;border-radius:10px;background:#fff7ed;color:#c2410c;font-weight:800;cursor:pointer}.directory-empty{padding:55px;text-align:center;border:1px solid #e2e8f0;border-radius:18px;background:#fff;color:#64748b}.directory-pagination{margin-top:20px}.directory-pagination nav>div:first-child{display:none}.snapshot-dialog{width:min(760px,calc(100% - 24px));max-height:90vh;padding:0;border:0;border-radius:20px;box-shadow:0 24px 80px rgba(15,23,42,.35)}.snapshot-dialog::backdrop{background:rgba(15,23,42,.7)}.modal-head{display:flex;justify-content:space-between;align-items:center;padding:19px 22px;background:linear-gradient(135deg,#172033,#f97316);color:#fff}.modal-head h2{margin:0;font-size:21px}.close-btn{border:0;background:transparent;color:#fff;font-size:28px;cursor:pointer}.modal-body{max-height:calc(90vh - 70px);overflow:auto;padding:22px}.snapshot-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.snapshot-item{padding:12px;border:1px solid #e2e8f0;border-radius:12px;background:#f8fafc}.snapshot-item.full{grid-column:1/-1}.snapshot-label{margin-bottom:4px;color:#64748b;font-size:11px;font-weight:800;text-transform:uppercase}.snapshot-value{color:#172033;font-size:14px;white-space:pre-line}.history{margin-top:18px}.history-entry{margin-bottom:8px;padding:10px 12px;border-left:3px solid #f97316;background:#fff7ed}.offer-form{margin-top:22px;padding-top:20px;border-top:1px solid #e2e8f0}.offer-form label{display:block;margin:12px 0 5px;color:#334155;font-size:13px;font-weight:800}.offer-form textarea{min-height:130px;resize:vertical}.offer-note{margin:8px 0 14px;color:#64748b;font-size:12px}@media(max-width:900px){.directory-filters{grid-template-columns:1fr 1fr}.directory-grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:580px){.directory-filters,.directory-grid,.snapshot-grid{grid-template-columns:1fr}.snapshot-item.full{grid-column:auto}}
.alumni-initial{overflow:hidden}.alumni-initial img{width:100%;height:100%;object-fit:cover}.snapshot-profile{display:flex;align-items:center;gap:16px;margin-bottom:16px;padding:14px;border:1px solid #fed7aa;border-radius:14px;background:#fff7ed}.snapshot-profile .alumni-initial{width:82px;height:82px;flex-basis:82px;font-size:26px}.snapshot-profile h3{margin:0 0 4px;color:#172033}.snapshot-profile p{margin:0;color:#64748b}
</style>
@endpush
@section('content')
<div class="directory">
<header class="directory-hero"><div class="directory-eyebrow">Talent Directory</div><h1>Alumni List</h1><p>Click an alumni name to view their professional snapshot and send a job offer.</p></header>
@if(session('status'))<div class="notice">{{ session('status') }}</div>@endif
<form class="directory-filters" method="GET" action="{{ route('employer.alumni_list') }}"><input name="search" value="{{ $search }}" placeholder="Search name or competencies" aria-label="Search alumni"><select name="course" aria-label="Course"><option value="">All courses</option>@foreach($courses as $option)<option value="{{ $option }}" @selected($course===$option)>{{ $option }}</option>@endforeach</select><select name="batch_year" aria-label="Batch year"><option value="">All batches</option>@foreach($batches as $option)<option value="{{ $option }}" @selected($batch===(string)$option)>{{ $option }}</option>@endforeach</select><button type="submit"><i class="fas fa-search"></i> Search</button></form>
@if($alumni->isEmpty())<div class="directory-empty"><h2>No matching alumni</h2><p>Try a different filter.</p></div>@else
<section class="directory-grid">@foreach($alumni as $person)
@php($photoUrl = $person->profile_picture ? url('/uploads/profiles/'.rawurlencode(basename($person->profile_picture))) : null)
<article class="alumni-card"><div class="alumni-card__top"><div class="alumni-initial">@if($photoUrl)<img src="{{ $photoUrl }}" alt="{{ $person->fullname }} profile picture" onerror="this.remove();this.parentElement.textContent='{{ mb_strtoupper(mb_substr($person->fullname,0,1)) }}'">@else{{ mb_strtoupper(mb_substr($person->fullname,0,1)) }}@endif</div><div><button class="name-btn" type="button" data-open-snapshot="snapshot-{{ $person->id }}">{{ $person->fullname }}</button><div class="alumni-card__course">{{ $person->course ?: 'Course not provided' }}</div></div></div><div class="alumni-meta">@if($person->batch_year)<span>Batch {{ $person->batch_year }}</span>@endif @if($person->employment_status)<span>{{ $person->employment_status }}</span>@endif</div><h3>Professional Competencies</h3><p>{{ $person->skills ?: 'No competencies listed.' }}</p><button class="view-btn" type="button" data-open-snapshot="snapshot-{{ $person->id }}"><i class="fas fa-user"></i> View Alumni Snapshot</button></article>
<dialog class="snapshot-dialog" id="snapshot-{{ $person->id }}"><div class="modal-head"><h2>Alumni Profile Snapshot</h2><button class="close-btn" type="button" aria-label="Close">&times;</button></div><div class="modal-body">
<div class="snapshot-profile"><div class="alumni-initial">@if($photoUrl)<img src="{{ $photoUrl }}" alt="{{ $person->fullname }} profile picture" onerror="this.remove();this.parentElement.textContent='{{ mb_strtoupper(mb_substr($person->fullname,0,1)) }}'">@else{{ mb_strtoupper(mb_substr($person->fullname,0,1)) }}@endif</div><div><h3>{{ $person->fullname }}</h3><p>{{ $person->course ?: 'Course not provided' }}{{ $person->batch_year ? ' · Batch '.$person->batch_year : '' }}</p></div></div>
<div class="snapshot-grid"><div class="snapshot-item"><div class="snapshot-label">Full name</div><div class="snapshot-value">{{ $person->fullname }}</div></div><div class="snapshot-item"><div class="snapshot-label">Course and batch</div><div class="snapshot-value">{{ $person->course ?: 'Not provided' }}{{ $person->batch_year ? ' · Batch '.$person->batch_year : '' }}</div></div><div class="snapshot-item"><div class="snapshot-label">Employment status</div><div class="snapshot-value">{{ $person->employment_status ?: 'Not provided' }}</div></div><div class="snapshot-item full"><div class="snapshot-label">Professional competencies</div><div class="snapshot-value">{{ $person->skills ?: 'Not provided' }}</div></div><div class="snapshot-item full"><div class="snapshot-label">Career objective</div><div class="snapshot-value">{{ $person->career_objective ?: 'Not provided' }}</div></div><div class="snapshot-item full"><div class="snapshot-label">Work experience summary</div><div class="snapshot-value">{{ $person->work_experience ?: 'Not provided' }}</div></div></div>
@if($person->employmentHistory->isNotEmpty())<div class="history"><h3>Employment History</h3>@foreach($person->employmentHistory as $job)<div class="history-entry"><strong>{{ $job->job_title ?: 'Role' }}</strong> at {{ $job->company_name ?: 'Company not provided' }}@if($job->employment_type)<br><small>{{ $job->employment_type }}</small>@endif</div>@endforeach</div>@endif
@if($person->education->isNotEmpty())<div class="history"><h3>Education</h3>@foreach($person->education as $education)<div class="history-entry"><strong>{{ $education->degree ?: 'Program' }}</strong><br>{{ $education->school_name ?: 'School not provided' }}</div>@endforeach</div>@endif
@if($person->certificates->isNotEmpty())<div class="history"><h3>Certificates</h3>@foreach($person->certificates as $certificate)<div class="history-entry"><strong>{{ $certificate->certificate_name }}</strong>@if($certificate->issue_date)<br><small>Issued {{ $certificate->issue_date->format('M Y') }}</small>@endif</div>@endforeach</div>@endif
<form class="offer-form" method="POST" action="{{ route('employer.offers.store') }}">@csrf<input type="hidden" name="alumni_id" value="{{ $person->id }}"><h3>Send Job Offer Email</h3><p class="offer-note">The message is sent to the address saved in this alumni account. That address remains private here.</p><label for="subject-{{ $person->id }}">Subject</label><input id="subject-{{ $person->id }}" name="subject" maxlength="255" required value="Job Opportunity from {{ auth()->user()->fullname }}"><label for="message-{{ $person->id }}">Message</label><textarea id="message-{{ $person->id }}" name="message" maxlength="5000" required placeholder="Describe the position, requirements, and how the alumni can contact you."></textarea><button class="primary-btn" type="submit"><i class="fas fa-paper-plane"></i> Send Job Offer</button></form>
</div></dialog>
@endforeach</section><div class="directory-pagination">{{ $alumni->links() }}</div>@endif
</div>
@endsection
@push('scripts')
<script>
document.querySelectorAll('[data-open-snapshot]').forEach(button=>button.addEventListener('click',()=>document.getElementById(button.dataset.openSnapshot)?.showModal()));
document.querySelectorAll('.snapshot-dialog').forEach(dialog=>{dialog.querySelector('.close-btn')?.addEventListener('click',()=>dialog.close());dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close()})});
</script>
@endpush

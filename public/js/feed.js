document.addEventListener('DOMContentLoaded',()=>{const toast=document.querySelector('[data-feed-toast]');const notify=message=>{if(!toast)return;toast.textContent=message;toast.classList.add('is-visible');clearTimeout(toast.timer);toast.timer=setTimeout(()=>toast.classList.remove('is-visible'),2400)};const escapeHtml=value=>{const node=document.createElement('div');node.textContent=String(value??'');return node.innerHTML};const initials=name=>String(name||'U').trim().split(/\s+/).slice(0,2).map(part=>part.charAt(0).toUpperCase()).join('')||'U';

document.addEventListener('click',event=>{const toggle=event.target.closest('[data-comment-toggle]');if(toggle){const section=document.getElementById(toggle.dataset.commentToggle);if(section){section.classList.toggle('is-collapsed');if(!section.classList.contains('is-collapsed'))document.getElementById(toggle.dataset.commentFocus||'')?.focus()}return}const jobToggle=event.target.closest('[data-job-toggle]');if(jobToggle){const list=document.querySelector('[data-job-list]');const expanded=list?.classList.toggle('is-expanded');jobToggle.textContent=expanded?'Show less':'Show more';return}const image=event.target.closest('[data-feed-image]');if(image){const box=document.querySelector('[data-feed-lightbox]');if(box){box.querySelector('img').src=image.dataset.feedImage;box.classList.add('is-open');box.setAttribute('aria-hidden','false')}return}if(event.target.closest('[data-feed-lightbox] > button')||event.target.matches('[data-feed-lightbox]'))closeLightbox()});

function closeLightbox(){const box=document.querySelector('[data-feed-lightbox]');if(!box)return;box.classList.remove('is-open');box.setAttribute('aria-hidden','true');box.querySelector('img').src=''}document.addEventListener('keydown',event=>{if(event.key==='Escape')closeLightbox()});

document.addEventListener('submit',async event=>{const form=event.target;if(form.matches('.reaction-form')){event.preventDefault();const response=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}});if(response.ok){const data=await response.json();form.closest('.post')?.querySelector('[data-counts]')?.replaceChildren(document.createTextNode(`${data.counts.total} reactions`))}return}if(form.matches('.feed-reaction-form')){event.preventDefault();const submitter=event.submitter;const reaction=submitter?.dataset.reaction||submitter?.value||'like';const body=new FormData(form);body.set('reaction_type',reaction);try{const response=await fetch(form.action,{method:'POST',body,headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}});if(!response.ok)throw new Error();const data=await response.json();const post=form.closest('.feed-post');const main=form.querySelector('[data-main-reaction]');const labels={like:['👍','Like'],love:['❤️','Love'],haha:['😂','Haha'],angry:['😡','Angry']};main.className='feed-action reaction-main is-'+(data.reaction||'none');main.value=data.reaction||'like';main.querySelector('[data-reaction-emoji]').textContent=labels[data.reaction||'like'][0];main.querySelector('[data-reaction-label]').textContent=data.reaction?labels[data.reaction][1]:'Like';post.querySelector('[data-count-label]').textContent=`${data.counts.total} reaction${data.counts.total===1?'':'s'}`;const stack=post.querySelector('.reaction-stack');stack.innerHTML=Object.entries(labels).filter(([key])=>data.counts[key]>0).map(([,info])=>`<b>${info[0]}</b>`).join('')}catch{notify('Reaction could not be saved. Please try again.')}return}if(form.matches('.feed-comment-form')){event.preventDefault();const input=form.querySelector('[name="comment"]');if(!input.value.trim())return;const button=form.querySelector('button[type="submit"]');button.disabled=true;try{const response=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}});if(!response.ok)throw new Error();const data=await response.json();const list=document.getElementById(form.dataset.commentsList);list.querySelector('[data-empty-comments]')?.remove();const c=data.comment;const row=document.createElement('div');row.className='feed-comment';row.dataset.commentId=c.id;row.innerHTML=`<div class="feed-avatar feed-avatar--small">${c.profile_photo?`<img src="/uploads/profiles/${encodeURIComponent(c.profile_photo.split('/').pop())}" alt="">`:escapeHtml(initials(c.fullname))}</div><div class="feed-comment__content"><div class="feed-comment__bubble"><strong>${escapeHtml(c.fullname)}</strong><p>${escapeHtml(c.comment)}</p></div><small>Just now</small></div>`;list.appendChild(row);input.value='';const counter=form.closest('.feed-post').querySelector('.feed-comment-count');counter.textContent=`${data.comment_count} comment${data.comment_count===1?'':'s'}`;notify('Comment posted.')}catch{notify('Comment could not be posted. Please try again.')}finally{button.disabled=false}return}});

const menu=document.querySelector('[data-mention-menu]');const users=window.gradconnMentionUsers||[];let mentionInput=null;let mentionStart=-1;function hideMentions(){if(menu)menu.hidden=true}function updateMentions(input){if(!menu)return;const pos=input.selectionStart||0;const before=input.value.slice(0,pos);const at=before.lastIndexOf('@');if(at<0||(at>0&&/\S/.test(before.charAt(at-1))))return hideMentions();const query=before.slice(at+1);if(query.length>40||/\n/.test(query))return hideMentions();const matches=users.filter(user=>String(user.name).toLowerCase().includes(query.toLowerCase())).slice(0,8);if(!matches.length)return hideMentions();mentionInput=input;mentionStart=at;menu.innerHTML=matches.map(user=>`<button type="button" data-mention-name="${escapeHtml(user.name)}"><span>${escapeHtml(initials(user.name))}</span>${escapeHtml(user.name)}</button>`).join('');const rect=input.getBoundingClientRect();menu.style.left=`${rect.left+scrollX}px`;menu.style.top=`${rect.bottom+scrollY+6}px`;menu.style.width=`${Math.max(rect.width,245)}px`;menu.hidden=false}document.addEventListener('input',event=>{if(event.target.matches('[data-mention-input]'))updateMentions(event.target)});document.addEventListener('click',event=>{const item=event.target.closest('[data-mention-name]');if(item&&mentionInput){const pos=mentionInput.selectionStart||0;const insertion='@'+item.dataset.mentionName+' ';mentionInput.value=mentionInput.value.slice(0,mentionStart)+insertion+mentionInput.value.slice(pos);mentionInput.focus();mentionInput.setSelectionRange(mentionStart+insertion.length,mentionStart+insertion.length);hideMentions()}else if(!event.target.closest('[data-mention-menu]')&&!event.target.matches('[data-mention-input]'))hideMentions()})});
